<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="uk.co.jordanrobinson.tracktospeech.MainActivity">

    <TextView
        android:id="@+id/now_playing_title"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:gravity="center"
        android:text="Now Playing"
        android:textSize="18sp"
        android:textColor="@color/DimGray"
        android:padding="10dp"/>

    <TextView
        android:id="@+id/track_view"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="10dp"
        android:layout_below="@id/now_playing_title"
        android:gravity="center"
        android:text="@string/text_default"
        android:textSize="24sp"
        android:textColor="@color/Black"
        android:padding="20dp"/>

    <ToggleButton
        android:id="@+id/on_off_toggle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/track_view"
        android:layout_centerHorizontal="true"
        android:paddingLeft="40dp"
        android:paddingRight="40dp"
        android:paddingTop="5dp"
        android:paddingBottom="5dp"
        android:textOff="Turn On"
        android:textOn="Turn Off"
        android:textSize="18sp" />

    <TextView
        android:id="@+id/history_title_view"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/on_off_toggle"
        android:gravity="center"
        android:text="History"
        android:textSize="18sp"
        android:textColor="@color/DimGray"
        android:padding="20dp"/>

    <ListView
        android:id="@+id/history_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/history_title_view" />

</RelativeLayout>
